mySQL

1. 중복성 제거 distinct
  * select 결과값에서 특정 컬럼만 출력할 경우 중복된 값이 나올 경우가 있다.
             이를 제거하기 위해 표시하는 기능.
  * select distinct 컬럼명1, 컬럼명2....... from 테이블명 where 조건절

2. 논리연산자 and, or, not. in. between

3. 결과 정렬 (order by)
 select 문의 결과값을 특정한 컬럼을 기준으로 
 오름차순/내림차순으로 정렬해서 표시.(기본값 오름차순)













